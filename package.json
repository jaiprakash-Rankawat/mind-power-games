{
  "name": "mind-power-games",
  "private": true,
  "type": "module",
  "description": "Chrome extension with brain-training games. No bundler: Chrome loads the source files directly, so these scripts only validate, package and serve.",
  "scripts": {
    "test": "node --test \"tests/*.test.mjs\"",
    "validate": "node tools/validate.mjs",
    "build": "powershell -ExecutionPolicy Bypass -File package.ps1",
    "dev": "python -m http.server 5173"
  }
}
